const product = (a, b) => {
    return (a * b);
};

const divide = (a, b) => {
    return (a / b);
};

const power = (a, b) => {
    return Math.pow(a , b);
}

console.log(product(8,4));
console.log(divide(8,4));
console.log(power(8,4));